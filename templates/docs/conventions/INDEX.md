# 컨벤션 단일 인덱스 — {{REPO}}

> 모든 컨벤션 문서는 **반드시 이 인덱스에 등록**한다. 여기 없는 컨벤션 문서는 고립 문서로 간주 — 금지.
> **이 파일의 이름·위치는 고정이다** — `.githooks/pre-commit` ⓒ 검사와 `guard-governance.sh`가 `docs/conventions/INDEX.md` 경로를 그대로 검사한다. 이름을 바꾸면 게이트가 조용히 헛돈다.

| 문서 | 대상 | 내용 |
|---|---|---|
{{CONVENTION_ROWS}}

<!-- 생성 안내(배치 후 이 주석은 지운다)
- 스택이 둘 이상이면 `conventions/` 아래를 하위 폴더로 분화해도 된다(예: `backend/`·`frontend/`). 그때도 등재는 이 INDEX.md 한 곳에 모은다.
- 인덱스 밖 위치에 예외 인정 자산이 있으면 "## 예외 인정 자산" 절을 만들어 등재한다 — 위치는 옮기지 않되 고립 문서로 두지 않기 위함.
- 규약이 아닌 참고 데이터(이슈 스냅샷 등)는 "## 참고 자료 (컨벤션 아님 — 등재만)" 절로 구분해 컨벤션과 섞지 않는다.
-->
