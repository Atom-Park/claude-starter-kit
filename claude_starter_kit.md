# 클로드 스타터킷 — 초안 (시작 베이스)

> **이 문서의 성격**: 완성된 스타터킷이 아니라 **킷을 만들어내기 위한 초안**이다.
> 여기에는 확정 원칙과 생성 흐름·요구사항만 있고, **문맥 의존 산출물의 실제 내용은 미리 완성하지 않는다** —
> 킷 산출물(`docs/conventions/*` · MEMBER-BOOTSTRAP 등)은 아래 §3 흐름을 따라 **생성 시점에 질문·답변을 반영해 작성**한다.
>
> **초기 상태 = 이 초안 + `templates/` 둘이다.** `templates/`는 **표준 문서 형태를 모아 둔 곳**이다 — 생성 시점에 참조해, *앞으로 개발하며 Claude가 읽게 될 문서*(허브·규칙·컨벤션 인덱스·constitution·부트스트랩)와 그 문서를 강제할 장치(훅·settings·PR 템플릿)를 같은 형태로 찍어낸다. **리포에 복사하지 않는다** — 생성이 끝나면 리포에서 읽을 주체가 없다. 강제 장치·골격(훅·settings·무조건 로드 규칙·**스킬 절차 골격**·PR 템플릿·허브/부트스트랩 골격)은 templates/ 원문을 **치환 복사**하고, 그 외 문맥 의존 문서(컨벤션·constitution·지도표)는 **이 초안을 통해 Claude가 생성**한다 — 검증이 축적된 실행물은 재생성하지 않는다.
> 초안은 **"생성 규약"**이다. 생성 이후의 값·규약 변경은 **`change-propagation` 스킬**(절차층 — templates 원문)이 절차를 갖는다.
>
> **실행 주체: 관리자(규칙 결정권자) — 이 문서 하나만 실행한다.** Claude Code에게 "claude_starter_kit.md 를 읽고 리포를 생성해라"라고 요청하면, **한 번의 실행으로 킷 확인/생성 → 리포 베이스 생성 → 팀원용 부트스트랩(MEMBER-BOOTSTRAP) 배치까지 완료**된다.
> 추가 리포도 `/kit-generate` 로 실행한다. 기존 리포의 개정 반영은 실행이 아니라 §3-B 경로다. **별도 관리자용 BOOTSTRAP 문서는 없다** — 그 절차는 §4-P로 이 초안에 내장된다.
> **팀원은 구조를 생성하지 않는다** — 팀원은 관리자가 배포한 리포를 clone 한 뒤, 리포에 포함된 **팀원용 부트스트랩(MEMBER-BOOTSTRAP.md)을 실행해 개인 셋팅만** 한다(플러그인 설치·개인 설정 — 실행 주체·배포: §3-B).
> **Claude**: 이 초안이 생성 규약이다. §1 전제를 지키고, §2 질문 후 §3 단계로 산출물을 작성한다.

---

## §0. 초안의 행동 규칙 (Claude 필독)

1. **복사와 생성의 구분** — 불변 강제 장치·골격(훅·settings·무조건 로드 규칙·**스킬 절차 골격**·PR 템플릿·허브/부트스트랩 골격)은 `templates/` 원문을 치환표대로 **복사**한다(임의 재작성 금지 — 실측 검증 자산 보존). 문맥 의존 문서는 templates 외의 어떤 원문도 전제하지 않고 §2 답변과 §4 요구사항으로부터 그 자리에서 **생성**한다.
2. **원칙은 전제, 내용은 생성** — §1의 원칙(3계층·방법론·운영원칙·산출물 규약)은 조정 대상이 아니다. 이 원칙을 만족하는 한 세부 내용(스택·규칙 문구·도구)은 답변에 맞춰 자유롭게 작성한다.
3. **단계마다 검증** — §3 각 STEP 끝의 ✔ 를 실행해 확인한다. 실패 시 수정 후 재시도(최대 3회), 안 되면 보류로 표시하고 완료 보고에 사유를 적는다.
4. **멱등** — 이미 있는 산출물은 덮지 않고 건너뛴 뒤 보고에 명시한다. **그래서 재실행은 기존 리포의 업그레이드 수단이 아니다** — 새 산출물만 만든다(개정 반영 경로는 §3-B).
5. **산출물 배치(§1)** — 리포 지식은 `docs/`(concepts·conventions·designs·constitution·personal)에, 변경 지도표는 `.claude-starter-kit/`에 둔다. **조직 공통 원칙의 정본은 이 초안 §1이다** — 리포에 요약 사본을 만들지 않는다(만들면 전파 대상이 하나 늘고 곧 어긋난다). Claude에게 자동 주입할 집행 규칙은 `.claude/rules/`에 둔다(§1 규칙 3층). **반복 실행 절차는 `.claude/skills/`에 스킬로 둔다**(호출 시에만 로드 — 상시 컨텍스트를 쓰지 않는다).
6. **실측 우선** — 도구 동작(훅 차단/자동수정, 템플릿 기본값, 설치 결과)은 문서로 단정하지 말고 **생성 시점에 실측**해 반영한다.
   (검증된 실측 예 ①: lint-staged `--fix` 훅은 포맷 위반을 **자동 수정 후 통과**시킨다 → 훅 "차단" 검증은 비수정 모드 도구나 자동수정 불가 위반으로 설계할 것.)
   (검증된 실측 예 ②: **린터의 종료코드 기준**을 반드시 잰다 — 위반을 warning 등급으로 다루는 린터는 위반이 있어도 `exit 0`이라 **게이트가 조용히 통과**한다. 자동수정 불가 위반을 실제로 만들어 스테이징하고 `sh .githooks/pre-commit` 이 `exit 1`을 내는지 확인한다. 통과해 버리면 경고를 오류로 올리는 옵션을 블록에 넣는다.)
7. **산출물 언어는 한국어로 확정** — 골격 27파일이 한국어 산문이고 `code-style.md`·`git.md` 가 이를 규칙으로 갖는다. 다른 언어가 필요하면 **골격 전체 번역이 선행**돼야 하며, 생성 시점에 바꿀 수 있는 값이 아니다. 예외는 **도구 표면**이다 — 브랜치명·PR 제목·파일/폴더 이름·코드 식별자, 그리고 **플러그인·명령 메타데이터**(`plugin.json`·`marketplace.json` 의 description, 명령 frontmatter)는 **영문**으로 쓴다. 사람이 읽는 산문이 아니라 **도구가 노출하는 식별·설명 문자열**이고, 설치 전 목록에서 언어를 못 읽는 사람도 판단할 수 있어야 한다.
8. **지침은 컨텍스트, 강제는 장치** — CLAUDE.md·`.claude/rules/`는 공식적으로 "강제된 구성이 아닌 컨텍스트"다. **지켜져야만 하는 규칙은 반드시 강제 장치와 짝으로 생성**하되, **장치별 유효 범위를 구분해** 짝을 설계한다: 권한 규칙 ask/deny는 **인터랙티브 모드에서 확인을 강제**하고, 무인·자동 승인 모드(`auto`·`dontAsk`·`bypassPermissions`)에서는 프롬프트 없이 자동 처리된다(실측) · PreToolUse 훅은 **권한 모드와 무관하게 실행**되며(서브에이전트 포함) exit 2 차단은 전 모드 유효 — 단 `permissionDecision:"ask"` 확인 강제는 무인 모드에서 자동 승인된다(실측 — 무인 방어는 감사·사후 보고로) · git pre-commit은 세션 밖 편집 포함 전원 적용(최종 안전망). **무인 모드의 보호 경로 방어는 훅·pre-commit 층이 담당한다.**

## §1. 확정 원칙 (킷이 반드시 담아야 하는 전제)

- **한 줄 원칙**: 지식은 리포에, 도구는 개인에, 규약은 조직에. **도구는 통일하지 않는다** — 표준화 대상은 방법론과 산출물 규약.
- **3계층**: ① 조직 공통층(스타터킷) → ② 리포층(리포 커밋: CLAUDE.md 허브·컨벤션·채택 설계·constitution) → ③ 개인층(로컬 gitignore: 워크플로우 도구·세션 문서·**개인 자산 폴더 `docs/personal/`** — README만 커밋 · **Claude 자동 메모리**). 이동은 한 방향 — **채택분만 승격(커밋)**.
- **자동 메모리 경계**: 판별 질문은 하나다 — **"다른 팀원의 Claude도 이걸 알아야 하는가?"** 예면 **커밋 문서(②)**, 아니오면 **자동 메모리(③)**. 자동 메모리에는 **그 사람·그 기계에만 해당하는 것**(작업 선호·받은 교정 피드백·개인 환경 사실)만 둔다. **팀 규약·컨벤션·구조·리포 진행 사실을 자동 메모리에 복제하지 않는다** — 전파가 닿지 않는 사본이 하나 더 생기기 때문이다(전파 절차는 개인층을 **검색만 하고 수정하지 않는다**). 메모리 내용이 팀에 필요하다고 판단되면 커밋 문서로 **승격(PR)**한다. 규약을 바꾼 뒤에는 `/memory`에서 낡은 항목을 본인이 정리한다 — 전파 보고의 "개인층 매치" 목록이 그 입력이다.
- **공통 방법론 5**: ① 문서 주도 ② 컨벤션 기준 ③ 단계 분해·체크박스 ④ 검증 명령 내장 ⑤ 커밋 단위 통제. **골격(계획서 서식 등)은 킷이 갖지 않는다** — 워크플로우 플러그인이 자체 양식을 제공하고, 킷 양식은 실측상 채택되지 않았다. 킷은 *무엇을 담아야 하는가*(위 5항)만 규정하고 서식은 도구에 맡긴다.
- **운영 원칙 7**: ① CLAUDE.md = 유일한 진입 허브(커밋·전 자산 링크) ② 지식은 커밋, 세션 잡동사니는 로컬 ③ 도구 자유·산출물 규약 표준 ④ 컨벤션 단일 인덱스(누락·고립 금지) ⑤ constitution 시작 시 1회 작성(3~5개, 게이트 가동) ⑥ **리포당 1거버넌스** — 허브 1·컨벤션 인덱스 1·킷/지도표 1. 모노레포 하위 패키지에 킷을 재적용하지 않고, **서브 CLAUDE.md는 "얇은 위임 문서"**(그 폴더 특화 규칙만·허브에서 링크·자체 컨벤션 폴더 금지)로만 둔다. **하위 폴더에서 `/init` 실행 금지**(cwd에 새 CLAUDE.md를 만들며 설정·훅으로 막을 수 없음 — 규칙+커밋 게이트로 방어) ⑦ **규칙 3층 구조** — **정본**(`docs/conventions/`+단일 인덱스: 사람·PR 리뷰용 해설) → **자동 주입**(`.claude/rules/`: Claude용 집행 요약 — `paths` frontmatter 없으면 매 세션, 있으면 일치 파일 작업 시 로드) → **강제**(권한 ask/deny·PreToolUse 훅·git pre-commit). 세 층의 짝 관계는 지도표에 등재해 전파로 동기화한다. **기준과 절차를 가른다** — 규칙 3층은 "무엇이 기준인가"만 담고, 여러 단계로 반복 실행되는 **절차는 `.claude/skills/`(절차층)로 뺀다**(호출 시에만 로드되어 상시 컨텍스트를 쓰지 않는다). 새 절차가 생기면 허브·규칙 문서를 산문으로 늘리지 말고 스킬로 만든다.
- **산출물 배치(4층)**: **`docs/`** = 리포 지식층(concepts·conventions·designs·constitution·personal — 사람·Claude가 필요할 때 읽는다) / **`.claude-starter-kit/`** = 변경 지도표 1파일 / **`.claude/rules/`** = 자동 주입층(규칙 집행 요약 — 주제당 1파일) / **`.claude/skills/`** = 절차층(절차당 1폴더 `<이름>/SKILL.md`, 호출 시에만 로드). **조직 공통 원칙은 초안 §1 하나가 정본** — 리포에 사본 문서를 만들지 않는다. ※ 킷 폴더는 숨김(dot) 폴더 — **전수 검색 시 숨김 경로 포함 필수**(예: `rg --hidden`).
- **생성 파일·폴더 이름은 영문**: 숫자 접두 + kebab-case(예: `doc-governance.md`·`propagation-map-<리포>.md`·`code-style.md`). **내용은 한국어**(constitution ①)지만 **이름은 도구가 다루는 식별자**다. 한글 파일명은 `git ls-files`의 인용 처리·macOS NFD 정규화·훅의 경로 패턴 매칭에서 **조용히 어긋난다** — 전수 검색에서 빠지고 pre-commit도 같은 이유로 건너뛴다.
- **컨텍스트 예산**: 허브 CLAUDE.md·rules 각 파일은 **200줄 이하** 목표(공식 권장 — 길수록 준수율 하락). 넘치면 paths 규칙(`.claude/rules/`)·스킬(`.claude/skills/`)로 분리한다. `@import`는 조직화용일 뿐 시작 시 전부 로드되어 컨텍스트를 줄이지 않음을 인지한다.
- **보호 장치**: ⓐ 개인 도구·플러그인의 커밋본 CLAUDE.md 자동 덮어쓰기 방지(격리·원복) ⓑ **보호 경로 게이트** — 공통 참조 문서(루트·서브 CLAUDE.md·`docs/constitution.md`·`docs/conventions/**`·`.claude-starter-kit/**`·`.claude/rules/**`·`.claude/skills/**`·`.claude/settings.json`〔장치가 장치를 보호〕·`.githooks/**`·PR 템플릿·MEMBER-BOOTSTRAP)의 편집에 **ask 권한 규칙**(인터랙티브 모드 확인 강제 — 규칙은 **`Edit(경로)` 폼으로만** 작성한다: `Write(경로)` 폼은 파일 권한 검사에 무효이며 Edit 폼이 파일 편집 도구 전체를 커버한다〔실측〕) + **모드 인지 가드** — PreToolUse 감사 기록(`.claude/unattended-edits.log`) + PostToolUse 사후 보고 의무로 무인·자동 승인 모드의 보호 경로 편집을 기록하고 최종 보고·커밋 전 확인을 강제한다(훅의 확인 강제〔`ask` 결정〕도 무인 모드에선 자동 승인됨〔실측〕) ⓒ 시스템 기본 보호 경로(`.claude/`·`.git`·`.vscode` 등 — allow 규칙으로도 사전 승인 불가)를 중복 등재하지 않고 그대로 활용
- **세션·권한 모드 운영 원칙**: ⓐ **세션은 리포 루트에서 연다** — 프로젝트 `.claude/settings.json`(권한·훅)·rules는 **세션 시작 디렉토리 기준으로만 로드**되므로, 서브 폴더에서 열면 거버넌스 장치가 빠진다 ⓑ 기본 권한 모드는 `default`(Manual)를 표준으로 하고 `permissions.defaultMode`로 settings에 명시(변경은 팀 협의) ⓒ 규모 있는 변경은 **plan 모드 선행**(`/plan`·`Shift+Tab`) 후 계획 승인 → 실행 ⓓ `bypassPermissions`는 격리 환경(컨테이너·VM) 밖에서 사용 금지 ⓔ **병렬 작업은 worktree로 분리**(`claude --worktree <이름>`) — 같은 세션을 두 터미널에서 재개하면 기록이 섞인다 ⓕ 세션에 이름을 붙여 재개한다(`claude -n <작업명>`·`/rename`·`--resume <이름>`·PR 연동 재개 `--from-pr <번호>`) ⓖ **세션 부트스트랩** — SessionStart 훅(startup·clear 한정)이 킷 세션 식별·자가 점검(core.hooksPath)을 주입하고, Claude는 **이상 항목만 예외 기반으로** 사용자에게 확인·교정 요청한다(전부 정상이면 한 줄 인사 후 즉시 작업). SessionStart 훅 입력에는 권한 모드가 제공되지 않는다(실측) — 모드 인지 방어는 PreToolUse·PostToolUse 층이 담당한다.

## §2. 생성 전 질문 — 1차 (공통, AskUserQuestion)

| #   | 질문                     | 예시 선택지                                                              | 비고                                                                                                                               |
| --- | ------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| Q1  | 킷 적용 조직/팀 이름     | 자유 입력                                                                | 문서 표지·CLAUDE.md 템플릿에 반영                                                                                                  |
| Q2  | 지원할 스택              | FE(React+TS+Vite) / BE(Python+FastAPI) / 기타(Java·Spring 등 자유)       | 복수 선택 — 스택마다 컨벤션·규칙 파일 생성                                                                                        |
| Q3  | 표준 워크플로우 플러그인 | superpowers 플러그인(기본) / 기타 플러그인(자유 입력)                    | **플러그인 기준만** — 설치형 스캐폴드 도구(Spec-Kit 등)는 제외. **사용법 문서는 만들지 않는다** — 플러그인이 자체 제공하고, 팀 고유 접점(보관 규약·격리 절차)은 이미 guide·허브에 있다 |
| Q4  | 기존 컨벤션 자산 위치    | 경로 입력 / 없음                                                         | 있으면 생성 컨벤션에 **원문 반영**(표준과 다르면 차이를 보고), 없으면 **§4-S 표준표** 적용                                     |
| Q5  | 폐쇄망 여부              | 예 / 아니오                                                              | 예: 원격 훅·마켓플레이스 의존 제거, 로컬 대체 설계                                                                                 |
| Q6  | 팀이 쓸 플러그인/MCP     | 예: 라이브러리 문서 조회 / 브라우저 자동화 / 디자인 / 코드 리뷰 (자유)   | **용도별 표로 MEMBER-BOOTSTRAP STEP ②에 수록**(별도 문서를 만들지 않는다 — 팀원이 설치하며 읽는 자리가 정본이다). 역할로 조합을 지정하지 않는다(각자 하는 일에 맞춰 고른다)                  |
| Q7  | 리포명·한 줄 설명    | 자유 입력                                                                | **이번 실행에서 생성할 리포**(§4-P) — 치환표 반영                                                                              |
| Q8  | constitution 원칙 3~5개  | 자유 입력 / 기본값                                                       | 기본: ① 산출물 한국어 ② 커밋 전 lint+test ③ API 변경 스펙 선행                                                                     |
| Q9  | 리포 생성 참고 자료  | 경로/링크 입력 / 없음                                                    | **이번 리포(§4-P) 생성에 반영할 자료** — 있으면 직접 읽어 반영. Q4(킷 컨벤션 자산)와 구분                                      |
| Q10 | 타 코딩 에이전트 병용    | 없음(기본) / 있음(도구명)                                                | 있음: `AGENTS.md`를 정본으로 두고 CLAUDE.md 첫 줄 `@AGENTS.md` 가져오기 + Claude 전용 지침만 아래 추가하는 호환 구성. 근거: Claude Code는 AGENTS.md 네이티브 미지원(공식) — 이 import가 공식 브릿지. **병용이 없으면 전환하지 않는다** |

요청문에 이미 답이 있으면 그 항목은 묻지 않는다.
질문 도구(AskUserQuestion)는 **1회 4문항 한도** — 5문항 이상이 남으면 ① 환경 실측으로 갈음 가능한 항목을 실측·가정 처리하고 히스토리에 명시하거나 ② 복수 회로 분할한다.

## §2-B. 문서별 내용 질문 — 2차 (각 산출물 생성 직전)

> **각 문서를 작성하기 직전에, 그 문서에 들어갈 내용을 아래 표대로 묻는다** (해당 STEP당 AskUserQuestion 1회로 묶음).
> 1차 답·요청문에서 이미 확인된 항목은 다시 묻지 않는다. **미응답/"기본값" 선택 시 [기본] 열을 적용**하고 완료 보고에 명시한다.
> **답변은 자유 텍스트 또는 문서 경로 참조** — 기존 문서 경로를 답으로 던지면 Claude가 직접 읽어 반영한다(원문 우선, 상충 시 §1 원칙 유지·보고에 명시).

| 생성 대상                              | 물어볼 내용                                                                                                                                                           | [기본] (미응답 시)                                                                                                                               |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 공통 컨벤션·설정                       | ① CLAUDE.md 개요 골자 ② settings 권한 allow 추가/삭제 ③ gitignore 추가 경로 ④ constitution 기본 원칙 세트                                                             | ① placeholder ② **골격 `settings.json` 의 allow 그대로**(git 조회·add·린트·빌드·설치) ③ 없음 ④ 기본 3원칙                                                                                       |
| **`.claude/rules/*` (규칙 파일)**      | ① 무조건 로드 규칙(문서 거버넌스) 문구 확정 ② 경로 범위 규칙 파일 구성(주제·paths 패턴)                                                                               | ① §4 표준 거버넌스 규칙 ② 스택당 1파일(`rules/<스택>.md`, 해당 소스 트리 paths)                                                          |
| **`.claude/skills/*` (스킬)**          | ① 스킬로 뺄 반복 절차 목록 ② 절차별 리포 고유 주의사항(전파 시 놓치기 쉬운 것·이슈 소스)                                                                              | ① 변경 전파·커밋/PR 2종(templates 골격) ② 없음(생성 후 실행하며 축적)                                                                            |
| 스택 컨벤션·규칙 (스택마다)            | **§4-S 표준표를 보여주고** ① 표준 그대로 쓸지, 바꿀 항목이 있는지 ② 조직 확정 컨벤션 세부(표준 밖 규칙 추가) ③ 테스트 도입 시점(지금/협의 후)                          | ① **§4-S 표준표 그대로** ② 없음 ③ 지금 도입(의존성+헬스체크 1건)                                                                                 |
| 리포 생성 STEP(§4-P)               | ① 방향(기능/정비/혼합) ② 완료 보고 추가 항목                                                                                                                          | ① 혼합 ② **§3 STEP 6 보고 형식**(생성 목록·검증 결과·기본값 적용·보류/미확정·배포 안내)                                                                                                                          |
| `MEMBER-BOOTSTRAP.md` (팀원용)         | ① 개인 셋팅 질문표 ② 개인 셋팅 검증 항목                                                                                                                              | ① 설치 범위(전체/안내만) ② **§4-M ⑤ 검증**(의존성·플러그인·hooksPath·개인 설정)                                                                                                                |

## §3. 생성 흐름 (STEP)

```
STEP 1  질문 확정(§2 — 1차 Q1~Q10 · 4문항 한도 규칙으로 분할)
STEP 2  변경 지도표 생성 (멱등 — 이미 있으면 재사용·스킵 보고)
        .claude-starter-kit/propagation-map-<리포>.md — 형식·시드는 §4-G
        — 머리말에 생성 흔적을 적는다: `> 생성: claude-starter-kit v<플러그인 버전> (<날짜>)`
STEP 3  치환값 확정 — Q&A 답변을 templates/README 치환표의 값으로 정리(중간 문서를 만들지 않는다 — §4)
STEP 4  ★ 리포 베이스 생성 (§4-P 절차 — 같은 실행에서 즉시 수행)
        스캐폴드(git init 최우선) → templates 치환 복사 → 문맥 의존 문서 생성(§4 — 컨벤션·MEMBER-BOOTSTRAP·rules) → 도구·훅·가드 → CLAUDE.md·constitution 치환 → 검증·첫 커밋·게이트 차단 검증
STEP 5  자체 검증 — ✔ 아래 전 항목 (킷 + 리포)
STEP 6  완료 보고 — 생성 목록 · 검증 결과 · 기본값 적용 항목 · 보류/미확정 · 배포 안내(push — §3-B)
```

## §3-B. 실행 주체·배포 (운영 규약)

> 전제: **팀원들은 하나의 리포에서 공동작업한다.** 리포 생성은 관리자, 팀원은 합류·개인 셋팅·개발.

```
🟠 관리자 (구조 생성 전담 — 문서 1개 실행으로 종료)
    [리포마다]  초안 실행 1회 = 킷 확인/생성(멱등) → 리포 베이스 생성(§4-P)
                    → MEMBER-BOOTSTRAP 포함 배치 → 원격 리포로 배포(push)
        ▼
🔵 팀원들 (하나의 리포에서 공동작업)
    리포 clone
      → "MEMBER-BOOTSTRAP.md 를 읽고 개인 셋팅을 진행해라" 실행
      → 이후 공동작업: 같은 리포에서 브랜치·커밋·PR 규약·컨벤션·승격 규칙 전원 공통
      → 세션 규약: ⓐ 세션은 리포 루트에서 연다(설정·훅·rules 적용 조건) ⓑ 세션 이름 지정(claude -n <작업명>)
                    ⓒ 중단 작업은 --continue/--resume <이름>, PR 후속은 --from-pr <번호> ⓓ 병렬 작업은 claude --worktree
                    ⓔ 하위 폴더에서 /init 금지 — 새 CLAUDE.md 신설은 PR로만
```

- **팀원은 구조를 만들지 않는다** — 초안을 실행하지 않고, 커밋된 규칙(CLAUDE.md·컨벤션·constitution·rules·훅)을 직접 수정하지 않는다. 개선은 **PR로 제안**(결정: 관리자, 발의: 전원).
- **공동작업 정합 장치** — 훅(커밋 게이트)·보호 경로 ask 규칙·PreToolUse 가드·PR 템플릿·컨벤션 인덱스·CLAUDE.md·rules가 **N명의 팀원 + 각자의 Claude를 동일 규칙으로 정렬**한다. 강제 장치는 커밋 자산이므로 리포 루트에서 연 모든 세션에 자동 적용된다.
- **킷 개정이 기존 리포에 닿는 경로** — **초안 재실행이 아니다.** 재실행은 멱등이라 이미 있는 산출물을 전부 건너뛴다(§0-4) — 새 리포를 만드는 수단이지 기존 리포를 고치는 수단이 아니다.
  - **값·규약 변경** → `change-propagation` 스킬. 지도표에서 위치를 찾아 참조층·집행층·실물을 고친다.
  - **구조 개정**(층 신설·폐지, 장치 변경) → 킷 저장소에서 고쳐 **플러그인 버전을 올리고**, 관리자가 리포마다 **직접 반영**한다. 구조 개정을 grep 전파로 처리하지 않는다.
  - **새 리포** → 초안 1회 실행. **`templates/`가 쓰이는 유일한 시점**이다.
  - 리포 간 드리프트는 지도표 머리말의 **생성 흔적(플러그인 버전) 비교**로 감지한다.

**STEP 5 ✔ 자체 검증 (필수)**

- 상호참조 링크 전수 유효 — 코드펜스 안 FILE 블록 경로는 배치 기준이므로 제외
- **리포 고유 규약이 슬롯에 담겼는가** — 허브의 작업 규약·하지 말 것에 리포 고유 항목이 있으면 `{{REPO_WORK_RULES}}`·`{{REPO_DONTS}}` 자리에 있어야 한다. 골격 고정 항목 사이에 끼워 넣으면 **판 갱신 때 조용히 사라진다**
- **서브 허브가 있으면 허브 문서 지도에서 링크됨**(운영 원칙 ⑥) — `{{SUB_HUB_LINK}}` 슬롯
- **생성기 지시문 잔존 0건** — 배포본에 `<!-- 생성 안내 ... -->` 주석이 남아 있지 않고, *"~는 이 절을 제거한다"·"(생성 시 작성)"* 류의 **생성기에게 하는 문장이 본문에 없다**. 골격에서 그런 지시는 전부 생성 안내 주석 형식으로만 쓴다
- **placeholder 양방향 대조** — ① 골격이 쓰는 `{{...}}`가 치환표에 전부 정의됨 ② 치환표에 있는데 **어느 골격에서도 안 쓰이는 고아 항목 0건**. ②를 빼면 골격을 고칠 때 죽은 치환자가 남는다
- §4-P·MEMBER-BOOTSTRAP 검증 명령이 §0-6 실측 원칙과 모순 없음
- §1 원칙 커버리지: 3계층·방법론5·운영원칙7·보호 장치·세션 운영이 산출물 어딘가에 모두 존재
- **플러그인 커버리지**: Q6의 모든 항목이 MEMBER-BOOTSTRAP STEP ② 표에 용도·설치 방법과 함께 등재되고, Claude가 설명한 뒤 설치하도록 안내함
- **배치 준수**: 리포에 조직 공통 원칙 사본 0건
- **리포 밖 경로 비노출**: git 추적 문서 전수에 리포 밖 경로 0건 — `git ls-files '*.md'` 대상 grep 실측. **표기 변형을 모두 덮는 패턴을 쓴다**(홈·워크스페이스·드라이브 문자 / 대소문자 / `\` `/` 슬래시 방향 / `../` 이탈 — 한 형태만 검색하면 통과처럼 보인다)
- **팀원 부트스트랩 연동**: MEMBER-BOOTSTRAP이 생성되고 STEP ② 플러그인 표가 Q6 답변을 빠짐없이 담음
- **〔신설〕 컨텍스트 예산**: 허브 CLAUDE.md·각 rules 파일 200줄 이하
- **〔신설〕 고립 규약 문서 0**: 컨벤션 인덱스 밖 규약 문서(서브 프로젝트 `doc/`·`docs/` 등) 0건
- **〔신설〕 규칙 3층 등재**: rules ↔ conventions 짝이 지도표에 등재됨
- **〔신설〕 절차층 배치**: 스킬이 `.claude/skills/<절차>/SKILL.md` 로 존재하고 templates 원문과 치환부 외 일치 · 보호 경로 목록(settings ask·가드·사후 보고 훅·doc-governance)에 `.claude/skills/**` 등재 · 각 SKILL.md 200줄 이하 · 허브 문서 지도에서 링크됨
- **〔신설〕 강제 장치 실측**: 보호 경로 ask 규칙(Edit 폼)·defaultMode가 settings에 존재 + 가드 차단 실측(화이트리스트 외 `CLAUDE.md` 생성 시도 → PreToolUse 차단 / 커밋 시도 → pre-commit 차단)
- **〔신설〕 린트 게이트 차단 실측**: 자동수정 불가 위반을 만들어 스테이징하고 pre-commit이 `exit 1`로 차단하는지 확인한다. `exit 0`이면 게이트가 무력한 것이다 — 종료코드 옵션을 고쳐 다시 잰다
- **〔신설〕 장치 서술 대조**: 문서가 설명하는 게이트 동작이 `.githooks/*` 원문과 일치하는지 확인한다 — 골격 문구를 그대로 옮겨 실제와 어긋나는 일이 잦다
- **〔신설〕 템플릿 원문 일치**: templates/ ↔ 배치 결과(훅·settings·rules·스킬·PR 템플릿) diff = 치환부 외 0건
- **생성 흔적 기록** — 지도표 머리말에 `생성: claude-starter-kit v<버전> (<날짜>)` 가 있다
- **킷 층 최소** — `.claude-starter-kit/` 에 지도표 1파일만 있다(초안 사본·골격 없음)

## §4. 산출물별 요구사항 (내용이 아니라 충족 조건)

### §4-P 리포 생성 STEP (초안 내장)

- 초안 STEP 4에서 같은 실행 안에서 즉시 수행. 입력은 1차 질문에서 확보 — 추가 질문 없음. Q9 자료 반영 규칙 동일.
- 행동 규칙: 해석 금지·복사 원칙 / 치환표 / 단계 검증(재시도 3회→보류) / 멱등 / 폐쇄망 대응
- 절차: 워크스페이스 생성 → `git init -b main` 최우선 → **templates/ 치환 복사**(훅·settings·doc-governance 규칙·**스킬 골격**·PR 템플릿·허브/부트스트랩 골격 — 치환표는 templates/README) → 문맥 의존 문서 생성(§4 — 컨벤션·MEMBER-BOOTSTRAP·**`.claude/rules/` 규칙 파일**·**`.claude/skills/` 치환값**) → 도구 설치·훅(**PreToolUse 가드·SessionStart 부트스트랩 등록** 포함) → **지도표 초기본 생성**(`.claude-starter-kit/propagation-map-<리포>.md` — 형식·시드는 §4-G) → CLAUDE.md·constitution 치환 → 최종 검증 → main 첫 커밋 → **게이트 차단 검증**(린트 훅 + 거버넌스 가드 — §0-6 실측 원칙 준수)

### §4-G 변경전파 지도표 — 형식·최초 생성·유지

- **성격**: 리포 고유 산출물. 리포 `.claude-starter-kit/propagation-map-<리포>.md` **1개이고 이 파일이 정본**이다. 전파 **절차**는 `change-propagation` 스킬이 갖고, 지도표는 그 절차가 조회하는 **사실 목록**이다 — 절차와 목록을 한 문서에 합치지 않는다.
- **스키마**:

```markdown
# 변경 전파 지도표 — <리포명>

> 생성: claude-starter-kit v<플러그인 버전> (<날짜>)
> 대상 리포: <리포 이름>(이 리포) · † = 협의 중 항목(확정 시 1순위 일괄 반영). 경로는 리포 루트 기준.

| 사실 | 현재 값 | ① 참조층 (Claude가 읽는 것) | ② 집행층 (장치가 읽는 것) | ③ 실물 |
|---|---|---|---|---|

## 리포 특이사항
- (실물 층 존재 여부 · 외부 기준(source of truth) 관계 · 리포별 주의점)
```

- **최초 생성 — 시드 3종**으로 초기 행을 뽑는다:
  1. **치환표 시드** — `templates/README.md` 치환표의 placeholder 전 행. placeholder = 킷이 스스로 선언한 "여러 곳에 퍼지는 사실"이므로 그대로 지도표 후보다. 각 placeholder의 현재 값으로 검색해 참조층·집행층·실물 위치를 채운다.
  2. **협의 표시 시드** — 산출물에서 "협의 필요"·"협의 중"·"미확정" 표시가 붙은 항목. 가장 먼저 바뀔 값들이므로 † 를 달아 등재한다.
  3. **유형 체크리스트 시드** — 포트 · URL/경로 규약 · 패키지/네임스페이스 · 외부 의존성 좌표·버전 · 커밋/PR 규약 · 플러그인 목록 · 스택·버전 · 기동/테스트 명령 · **Claude 규칙 파일(rules ↔ conventions 짝)** · **스킬층** · 보호 경로 목록 · 기본 권한 모드(defaultMode)
- **유지**: 전파 중 지도표에 없던 사실을 고쳤으면 행을 추가하고, 등재됐지만 실제와 다른 위치(문서 이동·삭제)를 발견하면 그 자리에서 바로잡는다. 유지비용은 실행에 내장 — 별도 관리 작업을 두지 않는다.

### §4-M MEMBER-BOOTSTRAP.md — 팀원 개인 셋팅 실행 규약

- **문서로 둔다 — 스킬화하지 않는다.** 온보딩은 1인당 1회이고, **Claude를 켜기 전에 사람이 읽는 첫 접점**이다(리포 페이지에서 바로 보이는 자리). 절차층(`.claude/skills/`)은 반복 실행되는 절차의 자리다.
- **플러그인 표는 여기 두지 않는다** — 정본은 `docs/conventions/plugins.md` 이고 STEP ② 는 링크만 한다.
- 실행 주체·질문·STEP 구성: ① 의존성 → ② 플러그인 설치 → ③ 설치 직후 안전 확인 → ④ 개인 설정 → ⑤ 검증 → ⑥ 보고. **STEP ②는 목록만 던지지 말고 각 플러그인이 무엇을 주는지 설명한 뒤 설치 의사를 확인**한다. 역할을 묻지 않는다.
- **〔신설〕 STEP ④에 세션 규약 안내 포함**: 세션은 리포 루트에서 열기(이유: 프로젝트 설정·훅·rules는 시작 디렉토리 기준 로드) · 세션 이름 지정·재개(`-n`·`--resume`·`--from-pr`) · 병렬은 `--worktree` · 하위 폴더 `/init` 금지.
- 구조 변경 금지 게이트 유지. 모노레포에 pnpm 패키지가 여럿이면 **STEP ①은 전 패키지 의존성 설치를 포함**한다.

### §4-S 스택 표준표 — 업계 표준 고정값

> **생성기는 이 표를 그대로 쓴다.** "검증된 표준"이라고만 적고 값을 지어내지 않는다.
> 관리자가 다른 값을 원하면 §2-B 스택 질문에서 **바꿀 항목만** 답하고, 바뀐 값은 지도표에 등재한다.
> 표에 없는 스택이면 **그 생태계의 사실상 표준을 근거와 함께 제시하고 확인받는다** — 임의로 정하지 않는다.

**도구**

| 스택 | 린터 (기본 강도) | 포매터 | 줄 길이 | 타입 검사 | 테스트 |
|---|---|---|---|---|---|
| TypeScript / React | `oxlint`(경고 승격 옵션 필수) 또는 `eslint` + `typescript-eslint` recommended | `prettier` 기본값 | 80 (prettier `printWidth` 기본) | 컴파일러 — `tsc --noEmit` | `vitest`(Vite 스택) 또는 `jest` + Testing Library |
| Python | `ruff check` — 기본 규칙 `E4,E7,E9,F` | `ruff format` (Black 호환) | 88 (Black·ruff 기본) | 별도 도구 — `mypy` 또는 `pyright` | `pytest` |
| Java / Spring | Checkstyle — Google Java Style | `spotless` + google-java-format | 100 (Google Java Style) | 컴파일러 | JUnit 5 |
| Go | `go vet` + `golangci-lint` | `gofmt` (설정 없음 — 언어 표준) | 제한 없음 (gofmt 규약) | 컴파일러 | 표준 `testing` |

**구조 — 소스가 한 트리라고 가정하지 않는다**

| 스택 | 소스 배치 | 테스트 배치 | 의존성·빌드 도구 | 버전 고정 |
|---|---|---|---|---|
| TypeScript / React | `src/**` | 소스 옆 `*.test.ts(x)` | npm/pnpm/yarn + `package.json` | `.nvmrc` · `packageManager` 필드 |
| Python | `src/<패키지>/**` (src-layout) 또는 `<패키지>/**` | **별도 `tests/`** (pytest 관례) | uv / poetry / pip + `pyproject.toml` | `.python-version` · `requires-python` |
| Java / Spring | `src/main/java/**` | **`src/test/java/**` 미러** (Maven Standard Directory Layout) | Gradle / Maven | toolchain 선언 (`build.gradle`·`pom.xml`) |
| Go | `cmd/**` · `internal/**` · `pkg/**` | 소스 옆 `*_test.go` | go modules + `go.mod` | `go.mod` 의 `go` 지시자 |

- **테스트 배치는 스택마다 다르다** — 한 스택의 관례를 다른 스택에 옮기지 않는다. 소스 옆(TS·Go) / 별도 트리(Python·Java)가 갈린다.
- **줄 길이는 린터와 포매터에 같은 값**을 넣는다. 다르면 두 도구가 서로의 결과를 되돌린다.
- **린터의 종료코드 기준을 반드시 확인한다**(§0-6 실측 예 ②) — 위반을 warning 등급으로 다루는 도구는 게이트를 조용히 통과시킨다.
- **버전은 표에 적지 않는다 — 생성 시점에 실측한다.** 이 표가 정하는 것은 *무엇을 쓰는가*이고, *어느 판인가*는 리포의 "검증된 조합" 표가 갖는다.
- 테스트 도입이 협의 대상이면 의존성만 넣고 "협의 필요"로 표시한다. **프레임워크 선택 자체는 위 표를 따른다.**

### 문맥 의존 문서 생성 (Q&A 답변 반영 — 중간 산출물 없음)

- **중간 문서를 만들지 않는다** — Q&A 답변을 받아 **대상 파일을 직접 쓴다.** 생성 지시를 모은 문서를 먼저 쓰고 그것을 적용하는 2단계를 두지 않는다. 그런 문서는 생성이 끝나면 **아무도 읽지 않으면서 값이 바뀔 때마다 고쳐야 하는 사본**이 되고, 실물과 어긋나도 열어보는 사람이 없어 드러나지 않는다. **요구사항은 이 절이, 골격은 `templates/`가, 결과는 실물이 갖는다** — 그 사이에 층을 더 두지 않는다.
- **치환값**은 문서로 남기지 않고 **`propagation-map-<리포>.md`에 사실 행으로 등재**한다(§4-G). 값이 여러 곳에 퍼지는 사실이라면 지도표가 그것을 추적할 자리다.
- **생성 대상 = 내용이 Q&A로 갈리는 문서뿐이다.** 구조·장치·문서 골격은 전부 `templates/` 치환 복사가 담당한다(목록: `templates/README.md` 파일 목록). 공통 생성 대상:
  - `docs/conventions/git.md` — **골격 치환 복사**(`templates/`). **type 표 11종은 고정 — 리포마다 바꾸지 않는다.** 치환 대상은 scope·브랜치 표·게이트 요약·PR 값뿐이다. Q4에 기존 자산이 있으면 scope·브랜치·게이트에 **원문 반영**하되, type 표는 골격을 유지하고 차이를 완료 보고에 적는다
  - `docs/conventions/code-style.md` — 공통 작성 규칙(주석 언어·문서 언어·구역 주석 등)
  - `docs/conventions/plugins.md` — **골격 치환 복사**. 플러그인·MCP 목록의 정본 — MEMBER-BOOTSTRAP STEP ② 는 이 문서를 링크만 한다(표를 두 곳에 두지 않는다). INDEX.md 등재 필수
  - **관리자가 Q4·Q9로 자료를 준 경우** — 그 자료가 규약이면 `docs/conventions/`에 컨벤션으로 만들어 INDEX 본표에 등재하고, 규약이 아닌 **참고 데이터**(이슈 목록·좌표 스냅샷 등)면 `docs/conventions/`에 두되 INDEX의 **"참고 자료(컨벤션 아님 — 등재만)" 절**로 구분해 등재한다. **킷이 스스로 만들 대상을 정하지 않는다** — 무엇이 필요한지는 관리자가 자료로 지정한다.
  - `.claude/settings.json`의 `allow` 목록에 스택 검증 명령 추가(치환)
  - `docs/conventions/INDEX.md`의 `{{CONVENTION_ROWS}}`·`docs/constitution.md`의 `{{CONSTITUTION_PRINCIPLES}}`·`docs/concepts/glossary.md`의 `{{REPO_GLOSSARY_ROWS}}` 채우기
- **templates가 담당하는 것 — 표준 문서 형태**(생성하지 않고 치환 복사한다):
  - **`.claude/rules/doc-governance.md`** (templates 원문) — paths 없는 무조건 로드 규칙(얇게): 새 CLAUDE.md·규약 문서 신설 금지(컨벤션은 docs/conventions+INDEX) · 하위 폴더 `/init` 금지 · 세션 리포 루트 · 공통 참조 문서 수정 게이트 요약 · **무인·자동 승인 모드에서 보호 경로 수정 금지**
  - **`.claude/settings.json`** (templates 골격) — `permissions.defaultMode` 명시 + 보호 경로 **ask** 규칙(§1 보호 장치 ⓑ 목록 — Edit 폼·settings 자기보호 포함) + 서브 컨벤션 폴더 신설 **deny** + 훅 등록 4종(**SessionStart 부트스트랩**〔startup·clear matcher〕·**PreToolUse 가드**·**PostToolUse 포맷/사후 보고**)
  - **`.githooks/protected-paths.sh`** (templates 원문) — 보호 경로 **단일 출처** `is_protected_path()`. 가드·사후 보고 훅이 source해 쓴다. 목록을 바꿀 때는 이 파일 + `settings.json`의 `ask` + `rules/doc-governance.md` 요약 **3곳을 같은 변경 단위로** 고친다(형식이 달라 완전 공유가 불가능하다). ※ `case` 패턴을 변수로 넘기면 값 안의 `|`가 대체 구분자로 해석되지 않으므로(실측) **목록 공유는 함수로만** 한다
  - **`.githooks/guard-governance.sh`** (templates 원문) — PreToolUse: 화이트리스트(루트·등록된 서브) 외 `CLAUDE.md` 생성·컨벤션 체계 밖 규약 문서 생성 exit 2 차단 + **무인·자동 승인 모드의 보호 경로 편집 감사 기록**(`.claude/unattended-edits.log` — gitignore 등재)
  - **`.githooks/notify-unattended-edit.sh`** (templates 원문) — PostToolUse: 무인 모드 보호 경로 수정 검출 시 exit 2 피드백(최종 보고 명시·커밋 전 사용자 확인 의무)
  - **`.githooks/session-bootstrap.sh`** (templates 원문) — SessionStart(startup·clear): 킷 세션 식별·자가 점검(hooksPath) 주입, 예외 기반 문답 지시
  - **pre-commit 거버넌스 3검사** (templates 원문 + 스택 린트 게이트 치환) — ⓐ 새 CLAUDE.md 화이트리스트 검사 ⓑ 인덱스 밖 규약 문서 차단 ⓒ `docs/conventions/` 신규 파일 ↔ INDEX.md 동반 수정 검사
  - **`.claude/skills/change-propagation/SKILL.md`·`.claude/skills/commit-and-pr/SKILL.md`** (templates 원문) — 절차층 기본 2종: 변경 전파(참조층→집행층→실물 6단계 · 이 킷의 변경 규약 전부를 담는다)·커밋/PR 실행(`docs/conventions/git.md` 정본 대조 → 게이트 통과 → PR 템플릿 충족). 치환값은 지도표에 등재한다(`{{MAP_TABLE}}`·`{{REPO_PITFALLS}}`·`{{ISSUE_SOURCE}}`·`{{TYPE_SCOPE}}`·`{{PR_SECTIONS}}`·`{{MERGE_COND}}`) — 절차 문구를 다른 문서에 복제하지 않는다. **새 절차가 필요하면 문서를 늘리지 말고 스킬을 추가**하고 보호 경로·허브 문서 지도에 함께 등재한다
  - **(Q10 "있음" 시)** CLAUDE.md 템플릿 첫 줄 `@AGENTS.md` 가져오기 구성
- 스택별 생성 대상: ① **검증된 조합**(런타임·프레임워크·빌드·린터/포매터·테스트의 버전 표 — `docs/conventions/<스택>.md`에 두며 이 표가 스택 버전의 단일 출처) ② `docs/conventions/<스택>.md` ③ templates 치환자 값 — **린터·포매터 명령 치환자(`{{LINT_FIX_BLOCK}}`·`{{FORMAT_CMD}}`)는 바이너리가 해석되는 패키지 루트에서 실행하도록 채운다**(모노레포 루트에서 바로 부르면 조용히 무동작). 스테이징 경로는 리포 루트 기준이므로 접두를 제거해 넘긴다. **게이트가 실제로 차단하는지 종료코드로 확인하고**(§0-6 실측 예 ②), 통과해 버리면 경고를 오류로 올리는 옵션을 넣는다 — **커밋 게이트와 검증 명령(`{{VERIFY_CMDS}}`)의 종료코드 기준을 같게 맞춘다**: `{{PKG_MGR}}`·`{{STAGED_SRC_REGEX}}`·`{{LINT_FIX_BLOCK}}`·`{{FORMAT_CMD}}`·`{{DEP_INSTALL}}`·`{{VERIFY_CMDS}}`·`{{EDITORCONFIG_STACK_BLOCK}}`·`{{GITIGNORE_STACK_BLOCK}}`) + **〔신설〕 경로 범위 규칙 파일** — `.claude/rules/<스택>.md`. frontmatter 형식은 다음으로 고정한다:
    ```
    ---
    paths:
      - <소스 트리 glob>
    ---
    ```
    `paths`는 **YAML 리스트**이고 값은 리포 루트 기준 glob(`<소스 트리>/**` 꼴)이다. `paths`가 없으면 매 세션 로드되므로, 경로 범위 규칙에는 반드시 넣는다: 그 스택 컨벤션의 **집행 요약**(정본 `docs/conventions/<스택>.md` 링크 필수 — 내용 중복 최소화, 지도표에 짝 등재)

## §5. 미확정 항목 (팀 협의 대상)

> 결정된 항목은 이 목록에서 지우고 결과를 §1 규칙에 반영한다 — 이 목록에는 **아직 열린 것만** 남는다.

- [ ] 기존 리포에 킷을 얹을 때의 정비 적용 범위 — 해당 영역 담당자 협의 후
- [ ] 플러그인/MCP 전사 권장 조합 확정 (폐쇄망 등록소 경유 방안 포함)
- [ ] 전사 강제 배포 — 관리 정책 CLAUDE.md·`managed-settings.json`(MDM/그룹정책 — `disableBypassPermissionsMode` 포함) 도입 검토
- [ ] **CI형 PR 게이트·예약 실행 도입 검토** — PR 자동 코드/보안 리뷰(claude-code-action 등)·야간 CI 점검. 강제 계층(권한·훅 / pre-commit / CI) 중 현행 공백인 **팀 수준 게이트** — 세션 밖·타 도구 편집을 닫는 마지막 층. 폐쇄망(Q5) 제약 확인 선결
